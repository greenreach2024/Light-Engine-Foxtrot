EB#545 - Block demo fallback on checkout + payment verification guards

CRITICAL SAFETY FIX:
- Remove entire demo checkout path from POST /checkout/execute
  Previously: if network allocation didn't run, demo path would create
  an order with provider='demo', status='completed' — no real payment
  collected but order looked fulfilled.
- Replace with hard 503 block + admin email alert when checkout would
  have fallen to demo fallback
- Guard accounting ingestion: revenue and farm payables only recorded
  when payment.status is 'completed' or 'pending', not 'failed'
- Log warning when accounting is skipped due to failed payment
