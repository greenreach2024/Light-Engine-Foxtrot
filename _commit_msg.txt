Deploy #526 — E.V.I.E. audit hotfix

Fix 3 bugs found by post-deploy audit:
1. BLOCKER: SyntaxError in assistant-chat.js line 1222 — stray ${summariesBlock} in variable name prevented server from starting
2. healthCheck.js wrong import paths (../database.js -> ../config/database.js, ../logger.js -> ../utils/logger.js)
3. Streaming endpoint fired redundant 2nd GPT call even when content already available — now streams existing content directly, halving token cost on tool-calling conversations
