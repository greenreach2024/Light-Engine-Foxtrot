Deploy #531 — Seed manual inventory product name from crop registry

Product Name field now uses datalist populated from crop-registry.json (32 crops).
Selecting a crop auto-fills the Category dropdown (mapped to dropdown values).