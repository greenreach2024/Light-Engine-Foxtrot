Deploy #532 — Fix manual inventory: remove price fields, add unique constraint

- Removed Retail/Wholesale price fields from manual entry form + table
  (pricing managed by Crop Pricing page; wholesale by GreenReach Wholesale)
- Added UNIQUE index on (farm_id, product_id) so ON CONFLICT UPSERT works
- Dedup existing rows before creating unique index
- Updated form description to reference POS/sales terminal flow