Deploy #533 — Bridge Crop Pricing to inventory + POS checkout

- POST /api/inventory/manual now resolves retail_price & wholesale_price
  from Crop Pricing page (farmStore crop_pricing) when not explicitly provided
- POS checkout adds fallback: if farm_inventory retail_price = 0,
  looks up crop pricing before computing line totals
- Wholesale catalog inherits correct wholesale_price at write time
- Eliminates $0.00 pricing gap for manually entered inventory items