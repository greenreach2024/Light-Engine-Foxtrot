Fix $0 pricing in edge sync and wholesale catalog

- inventory.js: Edge sync now calls resolveCropPricing() when device
  sends $0 price, resolving retail/wholesale from Crop Pricing page.
  ON CONFLICT preserves existing non-zero prices instead of overwriting
  with $0 from edge.

- wholesale.js: DB catalog path fills missing wholesale_price and
  retail_price from Crop Pricing (farmStore) before building response.
  Prevents $0 products from appearing in buyer catalog.
