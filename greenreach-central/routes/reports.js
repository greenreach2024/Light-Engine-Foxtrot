/**
 * Reports Routes (stub)
 */
import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'Reports routes not yet implemented' }));
export default router;
