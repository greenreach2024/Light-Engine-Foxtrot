/**
 * Inventory Routes (stub)
 */
import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'Inventory routes not yet implemented' }));
export default router;
