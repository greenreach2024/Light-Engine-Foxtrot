/**
 * Alerts Routes (stub)
 */
import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'Alerts routes not yet implemented' }));
export default router;
