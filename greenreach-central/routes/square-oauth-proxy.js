/**
 * Square OAuth Proxy Routes (stub)
 */
import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'Square OAuth proxy routes not yet implemented' }));
export default router;
